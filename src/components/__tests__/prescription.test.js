import { render, screen, cleanup } from '@testing-library/react'
import { BrowserRouter, Route, Switch } from 'react-router-dom';
import Prescription from '../Prescription'



// test('should render Prescription component', () =>{
//     render(<BrowserRouter><Prescription/></BrowserRouter>);
//     const prescriptionElement = screen.getByTestId('prescription-1');
//     expect(prescriptionElement).toBeInTheDocument();
//     expect(prescriptionElement).toHaveTextContent("Please Enter Patient's Prescription Details:");
// })


test('test', ()=> {
    expect(true).toBe(true);
})